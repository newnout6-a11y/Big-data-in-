streamlit
sentence-transformers
qdrant-client
protobuf>=3.20.3,<5
pypdf
pymupdf
fastembed
python-docx
python-pptx
genanki
streamlit-agraph
reportlab
groq
python-dotenv
numpy
httpx>=0.27
boto3>=1.34
# Tier 1 OCR для тетрадей (CPU-only, без системных зависимостей)
rapidocr-onnxruntime
pillow
# OCR встроенных картинок-схем PDF (опционально). Pure-python, при первом запуске
# скачает модели ~70 MB. Тянет за собой torch + opencv ~1 GB.
easyocr
